plugins {
    id 'com.gradleup.nmcp.settings' version '1.4.4'
}

rootProject.name = 'van-java-core'

nmcpSettings {
    centralPortal {
        username = System.getenv('SONATYPE_USERNAME')
        password = System.getenv('SONATYPE_PASSWORD')
        publishingType = 'AUTOMATIC'
    }
}
